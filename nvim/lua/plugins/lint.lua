return {
  "nvim-lint",
  opts = {
    linters_by_ft = {
      markdown = {}, -- Empty table disables all linters for markdown
      -- Keep other filetypes as they were or remove them if not needed
    },
  },
}
